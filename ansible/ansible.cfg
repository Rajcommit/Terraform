[defaults]
inventory = inventory/aws_ec2.yml
host_key_checking = False
remote_user = ec2-user
private_key_file = ~/.ssh/myec2key.pem

[inventory]
become = True
enable_plugins = aws_ec2
become_method = sudo