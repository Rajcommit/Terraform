plugin: aws_ec2
regions:
  - ap-south-1
filters:
  tag:Name: miniserver-*
  instance-state-name: running
keyed_groups:
  - key: tags.Role
    prefix: role
compose:
  ansible_host: private_ip_address
  ansible_user: "'ec2-user'"
  ansible_ssh_private_key_file: "'~/.ssh/myec2key.pem'"